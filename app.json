{
  "name": "Madara File Sharing Bot",
  "description": "A Telegram bot for sharing and storing files, built with Pyrogram and MongoDB.",
  "repository": "https://github.com/yourusername/madara-file-share-bot",
  "keywords": ["telegram", "file-bot", "pyrogram", "mongodb", "madara"],
  "env": {
    "API_ID": {
      "description": "Your Telegram API ID (from https://my.telegram.org)",
      "required": true
    },
    "API_HASH": {
      "description": "Your Telegram API Hash (from https://my.telegram.org)",
      "required": true
    },
    "BOT_TOKEN": {
      "description": "Bot token from @BotFather",
      "required": true
    },
    "OWNER_IDS": {
      "description": "Comma-separated Telegram user IDs of bot owners (e.g., 123456789,987654321)",
      "required": true
    },
    "DB_CHANNEL_ID": {
      "description": "Telegram channel ID for storing files (e.g., -1001234567890)",
      "required": true
    },
    "MONGO_URL": {
      "description": "Your MongoDB connection URI",
      "required": true
    }
  },
  "buildpacks": [
    {
      "url": "heroku/python"
    }
  ],
  "formation": {
    "worker": {
      "quantity": 1,
      "size": "free"
    }
  },
  "addons": [],
  "scripts": {
    "postdeploy": "echo '✅ Madara File Sharing Bot deployed successfully!'"
  }
}
